# Generated by ocuspot.
# Install this on container carrier "backend-kvm" and include it from the local rathole client config.

[client.services.ocuspot-deploy-demo]
type = "tcp"
local_addr = "127.0.0.1:8080"
token = "${OCUSPOT_RATHOLE_TOKEN}"
